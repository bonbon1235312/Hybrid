# Discord bot token and application ID. Keep real values in an uncommitted .env file.
DISCORD_TOKEN=
DISCORD_CLIENT_ID=
HYBRID_COMPONENT_SIGNING_KEY=
# Local Docker Compose default; replace for deployment without committing credentials.
DATABASE_URL=postgres://hybrid:hybrid@localhost:5432/hybrid
LOG_LEVEL=info
